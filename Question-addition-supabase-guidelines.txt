नमस्ते\! यह एक बेहतरीन *workflow* (कार्यप्रवाह) है। आपके *team members* (टीम के सदस्यों) के लिए *questions* (प्रश्न) *upload* (अपलोड) करने के लिए यहाँ एक *detailed guide* (विस्तृत गाइड) दी गई है।

यह *guide* (गाइड) मानती है कि *team member* (टीम का सदस्य) यह *process* (प्रक्रिया) पहली बार कर रहा है।

-----

## **Guide: Supabase में नए Questions कैसे Upload करें**

नमस्ते *Team* (टीम),

हमारे *Quiz App* (क्विज़ ऐप) में नए *questions* (प्रश्न) जोड़ने के लिए, कृपया इन *steps* (चरणों) का *follow* (पालन) करें। हमारा *process* (प्रवाह) यह *ensure* (सुनिश्चित) करता है कि सारा *data* (डेटा) *clean* (स्वच्छ) और *structured* (संरचित) हो।

*Process* (प्रवाह) है: **JSON File तैयार करें ➔ Python Script से CSV में बदलें ➔ Supabase में Upload करें।**

-----

### **Section 1: One-Time Setup (यह सिर्फ पहली बार करना है)**

यदि आपके *laptop* (लैपटॉप) पर *project environment* (प्रोजेक्ट एनवायरनमेंट) *set up* (सेट अप) नहीं है, तो पहले यह करें।

#### **Step 1.1: Python Install करें**

आपको *Python* (पायथन) की आवश्यकता होगी।

1.  [python.org/downloads](https://www.python.org/downloads/) पर जाएँ और *Python* (पायथन) का *latest version* (नवीनतम संस्करण) *download* (डाउनलोड) करें।
2.  *Install* (इंस्टॉल) करते समय, **"Add Python to PATH"** (पायथन को पाथ में जोड़ें) *checkbox* (चेकबॉक्स) को *check* (चेक) करना **सुनिश्चित करें**।

#### **Step 2.2: Project Folder बनाएँ**

1.  अपने *computer* (कंप्यूटर) पर एक *folder* (फ़ोल्डर) बनाएँ जहाँ आप *conversion* (रूपांतरण) का काम करेंगे। (जैसे: `D:\quiz-uploads`)।

#### **Step 1.3: Conversion Script (रूपांतरण स्क्रिप्ट) Save (सहेजें) करें**

1.  उसी `D:\quiz-uploads` *folder* (फ़ोल्डर) में, `convert_new_data.py` नाम की एक *file* (फ़ाइल) बनाएँ।
2.  उस *file* (फ़ाइल) में *exactly* (बिलकुल) यह *Python code* (पायथन कोड) *paste* (पेस्ट) करें। यह *script* (स्क्रिप्ट) *Hindi characters* (हिंदी वर्णों), *arrays* (ऐरे), और हमारे *full schema* (पूर्ण स्कीमा) ( `examDateShift` सहित) को *handle* (संभालने) के लिए *configure* (कॉन्फ़िगर) की गई है:

<!-- end list -->

```python
import json
import csv

# --- Configuration (कॉन्फ़िगरेशन) ---
# अपनी JSON फ़ाइल का नाम यहाँ बदलें
JSON_FILE_PATH = 'questions.json' 
# यह CSV फ़ाइल बनाएगा
CSV_FILE_PATH = 'questions_for_supabase.csv' 
# --- End Configuration (कॉन्फ़िगरेशन समाप्त) ---

def format_pg_array_correctly(py_list):
    """
    Python list को Supabase CSV Importer के लिए required format 
    (e.g., {"item1","item2"}) में बदलता है।
    """
    if not py_list:
        return "{}"
    
    formatted_list = []
    for item in py_list:
        # 1. आइटम को स्ट्रिंग में बदलें
        item_str = str(item)
        # 2. आइटम के अंदर मौजूद double quotes को escape करें (e.g., " becomes "")
        # PostgreSQL standard escaping for strings inside arrays
        item_str = item_str.replace('\\', '\\\\').replace('"', '\\"')
        # 3. CORRECTED LOGIC: हर आइटम को double quotes में wrap (रैप) करें
        formatted_list.append(f'"{item_str}"')
        
    return "{" + ",".join(formatted_list) + "}"

print(f"Starting conversion of {JSON_FILE_PATH} with CORRECTED script...")

try:
    with open(JSON_FILE_PATH, 'r', encoding='utf-8') as f_in, \
         open(CSV_FILE_PATH, 'w', encoding='utf-8-sig', newline='') as f_out:

        questions_data = json.load(f_in)
        
        header = [
            'v1_id', 'subject', 'topic', 'subTopic', 'examName', 'examYear','examDateShift',
            'difficulty', 'questionType', 'question', 'question_hi',
            'options', 'options_hi', 'correct', 'tags', 'explanation'
        ]
        
        writer = csv.DictWriter(f_out, fieldnames=header)
        writer.writeheader()

        count = 0
        for q in questions_data:
            try:
                # Use the new, corrected function
                options_pg = format_pg_array_correctly(q.get('options', []))
                options_hi_pg = format_pg_array_correctly(q.get('options_hi', []))
                tags_pg = format_pg_array_correctly(q.get('tags', []))
                
                explanation_jsonb = json.dumps(q.get('explanation', {}))

                writer.writerow({
                    'v1_id': q.get('id'),
                    'subject': q.get('classification', {}).get('subject'),
                    'topic': q.get('classification', {}).get('topic'),
                    'subTopic': q.get('classification', {}).get('subTopic'),
                    'examName': q.get('sourceInfo', {}).get('examName'),
                    'examYear': q.get('sourceInfo', {}).get('examYear'),
                    'examDateShift': q.get('sourceInfo', {}).get('examDateShift'),
                    'difficulty': q.get('properties', {}).get('difficulty'),
                    'questionType': q.get('properties', {}).get('questionType'),
                    'question': q.get('question'),
                    'question_hi': q.get('question_hi'),
                    'options': options_pg,
                    'options_hi': options_hi_pg,
                    'correct': q.get('correct'),
                    'tags': tags_pg,
                    'explanation': explanation_jsonb
                })
                count += 1
            except Exception as e:
                print(f"Error processing question {q.get('id')}: {e}")

    print(f"Successfully converted {count} questions to {CSV_FILE_PATH} using the correct format.")

except FileNotFoundError:
    print(f"Error: {JSON_FILE_PATH} not found.")
except Exception as e:
    print(f"An unexpected error occurred: {e}")
```

-----

### **Section 2: The Upload Process (यह हर बार करना है)**

जब भी आपके पास *upload* (अपलोड) करने के लिए नए *questions* (प्रश्न) हों, तो इन *steps* (चरणों) का *follow* (पालन) करें।

#### **Step 2.1: Prepare Your JSON File (JSON फ़ाइल तैयार करें)**

1.  आपके सभी नए *questions* (प्रश्न) (जैसे `ECO66`) एक *single JSON file* (एकल जेएसओएन फ़ाइल) में होने चाहिए।
2.  *Ensure* (सुनिश्चित) करें कि *file* (फ़ाइल) एक *Array* (ऐरे) हो (यानी, यह `[` से शुरू और `]` पर खत्म हो), जैसा कि आपके *example* (उदाहरण) में है।
3.  इस *file* (फ़ाइल) को *Rename* (नाम बदलें) करें: **`new_questions.json`**
4.  इस `new_questions.json` *file* (फ़ाइल) को अपने `D:\quiz-uploads` *folder* (फ़ोल्डर) में *copy* (कॉपी) करें।

#### **Step 2.2: Run the Conversion Script (स्क्रिप्ट चलाएँ)**

1.  **Command Prompt (cmd)** (कमांड प्रॉम्प्ट (सीएमडी)) खोलें।
2.  अपने *project folder* (प्रोजेक्ट फ़ोल्डर) पर जाएँ:
    ```cmd
    D:
    cd D:\quiz-uploads
    ```
3.  *Python script* (पायथन स्क्रिप्ट) को *run* (रन) करें:
    ```cmd
    python convert_new_data.py
    ```
4.  *Script* (स्क्रिप्ट) *run* (रन) होगी और एक *success message* (सफलता संदेश) दिखाएगी।
5.  आपके `D:\quiz-uploads` *folder* (फ़ोल्डर) में एक नई *file* (फ़ाइल) बन जाएगी: **`upload_this_file.csv`**

#### **Step 2.3: Upload CSV to Supabase (CSV को Supabase पर Upload करें)**

1.  अपने *Supabase project* (सुपाबेस प्रोजेक्ट) में *Login* (लॉग इन) करें।
2.  *Left sidebar* (बाएँ साइडबार) पर **Table Editor** (तालिका संपादक) (🗄️ आइकन) पर जाएँ।
3.  `questions` *table* (तालिका) पर *Click* (क्लिक) करें।
4.  *Table* (तालिका) के *top* (शीर्ष) पर, **"Insert"** (सम्मिलित करें) *button* (बटन) पर *click* (क्लिक) करें, फिर **"Import data from CSV"** (सीएसवी से डेटा आयात करें) चुनें।
5.  *Box* (बॉक्स) पर *click* (क्लिक) करें और अपनी `upload_this_file.csv` *file* (फ़ाइल) (जो `D:\quiz-uploads` में है) *select* (चयनित) करें।
6.  *Supabase* (सुपाबेस) *data* (डेटा) का *preview* (पूर्वावलोकन) दिखाएगा। *Ensure* (सुनिश्चित) करें कि *column headers* (कॉलम हेडर) सही *match* (मेल) कर रहे हैं।
7.  *Scroll* (स्क्रॉल) करें और *green* (हरे) **"Import data"** (डेटा आयात करें) *button* (बटन) पर *click* (क्लिक) करें।

-----

### **Section 3: Final Verification (अंतिम सत्यापन)**

1.  *Import* (आयात) *successful* (सफल) होने के बाद, `questions` *table* (तालिका) पर वापस जाएँ।
2.  *Table* (तालिका) के *bottom* (नीचे) *scroll* (स्क्रॉल) करें। *Row count* (पंक्ति गणना) बढ़ जानी चाहिए (जैसे, `914` से `915` या अधिक)।
3.  *New questions* (नए प्रश्नों) को *double-check* (दोहरा जांच) करने के लिए, `created_at` *column* (कॉलम) पर *click* (क्लिक) करके *table* (तालिका) को *Sort Descending* (अवरोही क्रम में क्रमबद्ध करें) (Z-A) करें।
4.  आपके द्वारा अभी *upload* (अपलोड) किए गए नए *questions* (प्रश्न) *top* (शीर्ष) पर दिखाई देंगे।
5.  *Check* (जांच) करें कि *Hindi text* (हिंदी टेक्स्ट) सही दिख रहा है और `options` (विकल्प) / `tags` (टैग) *columns* (कॉलम) में *grey "pills"* (ग्रे "पिल्स") (ऐरे) दिख रहे हैं।

-----

### **Troubleshooting (समस्या निवारण)**

  * **Error (त्रुटि): `violates not-null constraint` (नॉट-नल बाधा का उल्लंघन करता है)**
      * **Meaning (अर्थ):** इसका मतलब है कि आपकी `new_questions.json` *file* (फ़ाइल) में किसी *question* (प्रश्न) का एक *required field* (आवश्यक फ़ील्ड) (जैसे `subject` या `question`) *missing* (गायब) या *empty* (खाली) है।
      * **Solution (समाधान):** अपनी `new_questions.json` *file* (फ़ाइल) को *Fix* (ठीक) करें, *script* (स्क्रिप्ट) को फिर से *run* (रन) करें, और *new CSV* (नई सीएसवी) को *re-upload* (पुनः अपलोड) करें।